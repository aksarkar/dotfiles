general {
        color_bad = "#cb4b16"
        interval = 60
}

order += "battery 0"
order += "time"

battery 0 {
        format = "%status: %percentage"
        low_threshold = 30
        last_full_capacity = true
}

time {
     format = "%a %b %e %Y %H:%M"
}